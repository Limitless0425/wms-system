package com.example.demo.repository;

import com.example.demo.entity.RepackRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepackRecordRepository extends JpaRepository<RepackRecord, Long> {
}
